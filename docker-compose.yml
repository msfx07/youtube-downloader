services:
  web:
    build: .
    image: youtube-downloader
    container_name: youtube-downloader-localhost
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/downloads
    restart: unless-stopped
    